function locks(n){
  var lock = 0;
  while (n > 0){
    if (0 < x){ lock = 1 };
    if (0 < x){ lock = 0 };
    if (0 < x){ lock = 1 };
    if (0 < x){ lock = 0 };
    n = n - 1;
  }
  assert(lock == 0);
}
