//adapted from navier-stokes octane
class FluidField<M extends Mutable> {

    /*@ size : number */
    private size = 6;

    constructor() { }

    /*@ foo(x: {number | v = this.size}) : void */
    foo(x) {
        this.foo(x);
    }
}
