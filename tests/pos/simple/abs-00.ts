/*@ ab :: (number) => {res: number | res >= 0} */


// FIXME: the K-vared globs are still there ...

function ab(x:number):number{
  assert(false);
  return -1;
  //var r = x;
  //if (x > 2) {
  //  r = x;
  //} else {
	//r = (0 - x);
  //}
  //return r;
}
